module.exports = {
  // Specify the environments where your code will run
  env: {
    browser: true,
    node: true,
    es2021: true,
  },
  // Specify the parser options
  parserOptions: {
    ecmaVersion: 2021,
    sourceType: 'module',
  },
  // Specify the plugins your project requires
  plugins: [],
  // Specify the rules for your project
  rules: {
    // Example rules:
    // 'semi': ['error', 'always'],
    // 'quotes': ['error', 'single'],
    // 'indent': ['error', 2],
  },
};
